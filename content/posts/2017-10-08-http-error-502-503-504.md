---
title: 服务器返回 502、503、504 原因及解决办法
author: tanteng
type: post
date: 2017-10-08T14:44:08+00:00
url: /2017/10/http-error-502-503-504/
categories:
 - Develop
 - Nginx

---
HTTP 错误中的 502、503、504 是常见的网关错误，本文介绍这三种错误的原因及解决办法。

<!--more-->

### 502 Bad Gateway

**问题原因**：服务器作为网关或代理，从上游服务器收到无效响应。

Nginx 在这里充当的是反向代理服务器的角色，是把 HTTP 协议请求转成 FastCGI 协议的请求，通过 fastcgi_pass 指令传递给 php-fpm 进程，当 php-fpm 进程响应的内容是 Nginx 无法理解的响应，就会返回 502 Bad Gateway。

**解释**：出现 502 错误，通常意味着服务器已经不正确，简单说，就是机器挂掉了。Nginx 执行请求的时候，却收到了上游服务器的无效响应。

### 503 Service Unavailable

**问题原因**：

1. 请求用户量太多，服务器为了保护自己不挂掉，拒绝某些用户的访问，这些用户就会收到 503 错误
2. 以 PHP 作为后端语言的话，一个 HTTP 请求占用一个 php-fpm 进程，瞬时请求量过大时，没有足够的 php-fpm 进程去处理请求，就会返回 503 Service Unavailable

**解决办法**：等一会儿再访问该网站或者尝试强刷新页面，问题一般就能够解决了。

### 504 Gateway Timeout

**问题原因**：Nginx 或者后端配置不正确

**解释**：实际上 504 很少会遇到，通常这个错误是由于 Nginx 配置不当引起的。比如你将 Nginx 的超时时间设置为 300，那么如果此次请求的响应时间超过了 300，就会看到 504 这个报错。

**解决办法**：Nginx 或后端的配置参数设置正确合理。
